{
  "name": "browser-monads",
  "version": "0.1.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "test": "mocha",
    "build": "babel src -d lib",
    "prepublish": "yarn build && yarn test"
  },
  "dependencies": {
    "nothing-mock": "^1.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "chai": "^4.1.2",
    "mocha": "^5.0.5"
  }
}
